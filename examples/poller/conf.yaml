flasks:
  - modules: ['poller.app', 'poller.app.views']
  - modules: ['poller.views']
celeries:
  - modules: ['poller.tasks']
    config:
      broker_url: 'redis://'
      celery_result_backend: 'database'
      celery_result_dburi: 'redis://'
      celery_send_events: yes
sessions:
  db:
    url: 'sqlite:///db/db.sqlite'
    engine:
      echo: yes
twitter:
  user_handle: 'justinbieber'
